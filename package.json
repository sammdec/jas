{
	"name": "jas",
	"workspaces": ["packages/*"],
	"scripts": {
		"dev": "bun --filter 'ui' dev & bun --filter 'server' dev",
		"build": "bun run --filter 'ui' build"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.8.3",
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5.5.3"
	}
}
